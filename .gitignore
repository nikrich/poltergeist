# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Venv
.venv
venv/

# Logs
logs/

# Local env
.env
.env.local

# Editor
.idea/
.vscode/
*.swp
.DS_Store

# PyInstaller / desktop packaging artifacts
packaging/build/
desktop/resources/sidecar/

# Native macOS capture helper — built by scripts/build-native-macos.sh
desktop/resources/bin/
native/macos/ghostbrain-capture/.build/
native/macos/ghostbrain-capture/.swiftpm/

# Claude Code linked worktrees (skills under .claude/skills/ ARE tracked)
.claude/worktrees/
.superpowers/

# Demo recorder intermediates — regenerable via `npm run demo`. The committed
# deliverables are media/poltergeist-demo.{mp4,gif}; the raw webm + palette are not.
media/.raw/
media/poltergeist-demo.webm
.venv
